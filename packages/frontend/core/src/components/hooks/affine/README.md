# AFFiNE Hooks

> This directory will be moved to `@affine/worksapce/affine/hooks` in the future.

Only put hooks in this directory if they are specific to Ecodigital, for example
if they are using the Ecodigital API, or if the `EcodigitalWorkspace` is required.
